[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.5"
dependencies = [
    "celery>=5.4.0",
    "celery-prometheus-exporter>=1.7.0",
    "redis>=5.2.1",
]
